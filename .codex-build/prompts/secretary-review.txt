You are the RUST SECRETARY doing a FULL, RIGOROUS REVIEW of this Rust brain. Working dir = vault root.
First read AGENTS.md and wiki/meta/CONVENTIONS.md and wiki/concepts/Ownership.md (the standard).

GOAL: independently verify the whole brain is correct, current (edition 2024 / stable 1.85+), complete,
and consistent — then fix what is unambiguously safe and REPORT the rest. Be skeptical; do not rubber-stamp.

USE TOOLS AT SCALE. You have a shell — write and run grep/awk/python checks across all of wiki/ rather
than eyeballing 500+ files. Then deep-read SAMPLES for correctness.

REVIEW DIMENSIONS:
1. COMPLIANCE (mechanical, check ALL notes): every concept/pattern/antipattern has valid flat-YAML
   frontmatter (type,title,status,created,updated,tags,domain,difficulty,related,sources,source_urls,
   rust_version); exactly one H1; a ```rust example; >=6 [[wikilinks]]; "Sources" section. List violations.
2. CORRECTNESS & CURRENCY (deep-read a representative SAMPLE across every domain, prioritising unsafe,
   async, concurrency, traits/lifetimes, macros, std-*): is the Rust correct and idiomatic? Do code
   examples compile in principle? Any deprecated/outdated idioms presented as current (e.g. `&` on
   `static mut` instead of `&raw`; missing `unsafe extern`; wrong const-fn versions; thiserror/anyhow
   API drift)? Any factual errors? Cite file + line.
3. LINK & STRUCTURE HEALTH: compute dead [[wikilinks]] (resolve by filename OR frontmatter alias,
   ignore links inside code spans/fences); duplicate or near-duplicate notes across domains;
   contradictory notes; MOCs that omit notes in their domain.
4. COVERAGE COMPLETENESS ("is everything in?"): compare the brain against the source books in
   .raw/books/ and the std pages in .raw/std/. List notable MISSING topics a "complete" Rust brain
   should have (by domain). Be specific and prioritised.

FIX (only clearly-safe, unambiguous changes — make them in place):
- broken/missing frontmatter fields, wrong dates, malformed YAML
- obviously-incorrect or deprecated code idioms (replace with the current idiom)
- trivially-fixable dead links (add an `aliases:` entry to the right canonical note, or correct the link)
Do NOT do sweeping rewrites or delete notes. Do NOT touch .raw/, sources/, research/, rust/.

DELIVERABLE — write a report to wiki/meta/secretary-review.md with this structure:
  - frontmatter: type: meta, title: "Secretary Review", status: evergreen, created/updated 2026-06-21, tags
  - "## Verdict" (one paragraph + an overall PASS/PASS-WITH-FIXES/NEEDS-WORK)
  - "## Scorecard" (counts: notes checked, compliance violations, dead links, currency issues found/fixed)
  - "## Fixed" (bullet list of the safe fixes you applied, with file paths)
  - "## Findings" in tiers BLOCKER / HIGH / MEDIUM / LOW — each with file:line and a recommended fix
  - "## Coverage gaps" — prioritised missing topics by domain
Keep findings concrete and cite paths. At the end, print a short summary to stdout: counts + verdict + how many fixes you applied.

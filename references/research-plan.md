# Research Plan — Rust Brain

Sources: official Rust books (`.raw/books/`), verified deep-research (`research/`), `rust-lang/rust`.
Refresh: re-scrape books per 6-week release; re-run the research workflow monthly; sweep RustSec for security.
Trust hierarchy: official-doc > RFC > primary-research > first-party-blog > community.

# Current Requirements — Rust Brain (2026-06-21)

- Target: Rust edition 2024, stable 1.85+.
- Every atomic note: answer-first, compilable example, best-practice + pitfalls, >=6 wikilinks, cited sources.
- No deprecated idioms presented as current (e.g. `&raw` over `&` on `static mut`; `unsafe extern` blocks).
- Provenance tracked in `references/source-ledger.json` with refresh due dates.
